# K8s ConfigMap - feed-service
# 注意: 实际配置由 overlays 环境覆盖
apiVersion: v1
kind: ConfigMap
metadata:
  name: feed-service-config
  namespace: placeholder-namespace
  labels:
    app: feed-service
data:
  SPRING_PROFILES_ACTIVE: "placeholder-env"
