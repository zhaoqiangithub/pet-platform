# K8s Kustomization - feed-service
# 注意: 实际 namespace 由 overlays 环境覆盖
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: placeholder-namespace

resources:
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: feed-service
  version: v1
